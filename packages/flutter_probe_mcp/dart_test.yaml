# E2E tests require a running Flutter app + device/simulator.
# Default `dart test` skips them. To run them:
#   dart test --run-skipped --tags e2e
tags:
  e2e:
    skip: 'requires device/simulator — run with: dart test --run-skipped --tags e2e'
